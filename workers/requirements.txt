# Workers de dados/IA — dependências base.
httpx>=0.27           # chamadas HTTP às fontes (EAN, Icecat, SerpApi, feeds)
psycopg[binary]>=3.2  # Postgres
pydantic>=2.7         # validação/normalização de payloads das fontes
python-dotenv>=1.0    # carregar .env
tenacity>=8.2         # retries com backoff nas APIs externas
anthropic>=0.40       # IA (explicação/normalização/resumos), sempre ancorada em fontes
