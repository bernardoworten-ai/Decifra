[pytest]
markers =
    integration: testes e2e que precisam de Postgres (skip limpo sem BD)
# Por defeito, `pytest` corre só os testes unitários (offline).
# Para os e2e: `pytest -m integration`.
addopts = -m "not integration"
testpaths = tests
